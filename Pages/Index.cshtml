@page

@{
    ViewData["Title"] = Model.Title;
}

<style>
    /* 样式代码 */
</style>

<body>
<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card">
                <div class="card-header">@ViewData["Title"]</div>
                <div class="card-body">
                    <!-- 卡片内容 -->
                </div>
            </div>
        </div>
    </div>
</div>
<div class="jumbotron">
    <h1>LSP_HOME</h1>
    <p class="lead">Welcome to LSP's home! This is a place where you can find all kinds of interesting things!</p>
    <hr class="my-4">
    <p>Explore now:</p>
    <a class="btn btn-primary btn-lg" href="/blog">Blog</a>
    <a class="btn btn-secondary btn-lg" href="/gallery">Gallery</a>
</div>
</body>


<!-- HTML -->
<div id="datetime"></div>

<script>
function displayDateTime() {
   const datetimeElement = document.getElementById('datetime');
   const now = new Date();
   const year = now.getFullYear();
   const month = (now.getMonth() + 1).toString().padStart(2, '0');
   const day = now.getDate().toString().padStart(2, '0');
   const hour = now.getHours().toString().padStart(2, '0');
   const minute = now.getMinutes().toString().padStart(2, '0');
   const second = now.getSeconds().toString().padStart(2, '0');
   const datetimeString = `${year}-${month}-${day} ${hour}:${minute}:${second}`;
   datetimeElement.textContent = datetimeString;
}

setInterval(displayDateTime, 1000);
</script>

<!-- Visit Count Script -->
<script src="~/js/visitcount.js"></script>
<script>
    updateVisitCount();
</script>
